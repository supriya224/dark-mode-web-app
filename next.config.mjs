/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["next/image"] // it is allow to user show where it images exists in website and hostname as well
    }
};

export default nextConfig;
